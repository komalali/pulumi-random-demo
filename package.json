{
  "name": "pulumi-random-demo",
  "version": "1.0.0",
  "description": "A Pulumi program that creates a random string",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/random": "^4.0.0"
  }
}
